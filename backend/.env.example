# Copy to .env for local runs. On Render, set variables in the dashboard.

# --- MongoDB Atlas (required) ---
# SRV example: mongodb+srv://USER:PASS@cluster0.xxxxx.mongodb.net/quanton?retryWrites=true&w=majority
MONGODB_URI=

# --- HTTP ---
# Render sets PORT automatically. Local default is 5001 if unset.

# Browser origins allowed to call this API (comma-separated). Must include your Vercel URL.
CORS_ORIGINS=https://quanton-nine.vercel.app,http://localhost:5173

# Single-origin alias (optional if CORS_ORIGINS is set):
# FRONTEND_URL=https://quanton-nine.vercel.app

# --- Optional: gifts.json path (resolver + non-prod empty-DB demo seed; no auto Mongo seed in production) ---
# GIFTS_JSON_PATH=/absolute/path/to/gifts.json

# --- Telegram ---
# BOT_TOKEN=123456:ABC...
# ADMIN_CHAT_ID=123456789
# Same public URL as the Mini App (Vercel):
# MINI_APP_URL=https://quanton-nine.vercel.app

# --- Gift Asset API (Telegram gift metadata) ---
# https://github.com/GIFT-ASSET/gift_asset_api — Swagger security: header X-API-Key (Bearer/query supported here).
# GIFT_ASSET_BASE_URL=https://giftasset.gifts
# GIFT_ASSET_API_KEY=
# GIFT_ASSET_AUTH_MODE=bearer
# GIFT_ASSET_AUTH_MODE=x-api-key
# GIFT_ASSET_AUTH_MODE=api-key
# GIFT_ASSET_AUTH_MODE=query
# GIFT_ASSET_AUTH_MODE=legacy
# GIFT_ASSET_AUTH_HEADER=X-API-Key

# --- Metadata refresh (cron / admin) ---
# Required in production for POST /gifts/:listingId/metadata/refresh and POST /gifts/metadata/sync-stale
# METADATA_SYNC_SECRET=

# --- Danger: wipe all marketplace listings (MongoDB `gifts` collection only) ---
# Script: CLEAR_LISTINGS_CONFIRM=DELETE_ALL_MARKETPLACE_LISTINGS npm run clear-listings
# Production script: also pass --production or CLEAR_LISTINGS_ALLOW_PRODUCTION=1
# HTTP (optional): POST /admin/clear-listings with header X-Clear-Listings-Secret — only if secret is set.
# CLEAR_LISTINGS_SECRET=
# CLEAR_LISTINGS_ALLOW_HTTP_IN_PRODUCTION=1

# --- Optional: AI image upscale (Real-ESRGAN via Replicate; OpenGraph + gift-asset rasters) ---
# REPLICATE_API_TOKEN=r8_...
# AI_UPSCALER_ENABLED=true
# Optional: override default nightmareai/real-esrgan Replicate **version** id (64-char hash from replicate.com):
# REPLICATE_REAL_ESRGAN_VERSION=f121d640bd286e1fdc67f9799164c1d5be36ff74576ee11c803ae5b665dd46aa
# Cartoon / NFT / sticker-like rasters: Real-ESRGAN is general-purpose; try a Replicate **anime** variant
# (e.g. search replicate.com for "real-esrgan anime" and paste that model's version hash here), or diffusion
# upscalers (slower/costlier) such as lucataco/stable-diffusion-x4-upscaler — wire input/output in imageUpscaler.js.
# Legacy (still supported): AI_UPSCALER_API_KEY, AI_UPSCALER_MODEL, AI_UPSCALER_PROVIDER=replicate
# AI_UPSCALER_MIN_EDGE_PX=480
# AI_UPSCALER_MAX_DOWNLOAD_BYTES=4500000
# AI_UPSCALER_DOWNLOAD_TIMEOUT_MS=12000
# AI_UPSCALER_JOB_TIMEOUT_MS=45000
# AI_UPSCALER_MAX_RETRIES=2

# Optional: collection floor cache TTL for Gift Asset (60000–180000 ms, default 120000)
# FLOOR_CACHE_TTL_MS=120000

# Optional: override default probe collection for GET /debug/providers?probe=1
# GIFT_ASSET_PROBE_COLLECTION=Plush Pepe

# GET /debug/providers — open in development; in production returns 404 unless set and caller sends header X-Debug-Providers-Secret
# DEBUG_PROVIDERS_SECRET=

# --- Node ---
# NODE_ENV=development
