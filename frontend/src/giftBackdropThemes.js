/**
 * Telegram NFT collectible backdrop themes.
 *
 * Canonical gradients live in `shared/giftHeroThemes.json`.
 * Resolution + symbol slugs: `shared/giftHeroResolve.js`.
 *
 * @example
 * import { resolveBackdropTheme, resolveSymbolPattern } from "./giftBackdropThemes.js";
 */

export {
  normalizeTraitKey,
  resolveBackdropTheme,
  resolveSymbolPattern,
  buildHeroPresentationFields,
} from "@shared/giftHeroResolve.js";
